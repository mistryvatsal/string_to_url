from distutils.core import setup


setup(
  name = 'string_to_url',
  packages = ['string_to_url'], 
  version = '0.1',
  description = 'string_to_url converts simple string into string replaced by + in place of white spaces so that it can be used for query URLs. (UTF-8)',
  author = 'Vatsal Mistry',
  author_email = 'mistryvatsal11@gmail.com',
  url = 'https://github.com/mistryvatsal/string_to_url', 
  download_url = '', 
  keywords = ['URL', 'URL encoding', 'URL query encoding ', 'UTF-8', '+ instead of whitespaces','url', 'stringtourl' ], 
  classifiers = [], 
)